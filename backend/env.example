# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# For MongoDB Atlas (replace <db_password> with your password)
MONGODB_URI=mongodb+srv://admin:<db_password>@cluster0.r5ikkfl.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# JWT Secret (for future authentication)
JWT_SECRET=your-super-secret-jwt-key-here

# Email Configuration (for future newsletter functionality)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# File Upload Configuration (for future image uploads)
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880 